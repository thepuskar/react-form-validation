import './App.css'
import { useForm } from 'hooks'
import { RegisterForm } from 'components'
import { IFormData } from 'interface'

function App() {
  return (
    <div className='App'>
      <RegisterForm />
    </div>
  )
}

export default App
